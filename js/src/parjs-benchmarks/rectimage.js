// -*- mode: js2; indent-tabs-mode: nil; -*-

load(libdir + "rectarray.js");

// A RectImage is a subclass of RectArray<Byte, 4>.
// (Maybe later it will be a RectTypedByteArray<4>.)

function RectImage(w,h,fill)
{

}
